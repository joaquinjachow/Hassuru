{
  "name": "hassuru",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install --omit=dev && npm start",
    "dev": "cd backend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
